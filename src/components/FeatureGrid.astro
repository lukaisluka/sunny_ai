---
const { items = [], columns = 3 } = Astro.props;
---

<div class={`grid cols-${columns}`}>
  {items.map((item) => (
    <article class="card">
      <h3>{item.title}</h3>
      <p>{item.description}</p>
    </article>
  ))}
</div>
